# Dual-Domain TimesNet with STL Decomposition and Auto-Correlation for Long-Term Solar Forecasting

---

## 摘要

长期太阳能预测在可再生能源电网调度与能源管理中扮演着至关重要的角色。然而，太阳能时间序列呈现出显著的非平稳特性、复杂的周期结构以及跨周期的长滞后依赖，这对现有预测模型提出了严峻挑战。本文提出深度学习框架**Dual-Domain TimesNet**（简称TimesNet-Full），其三个模块分别作用于周期建模流程的**三个阶段**：**STL分解**在输入阶段将序列分离为趋势、季节性和残差三个分量，并由三个独立参数的嵌入网络分别表示；**FFT-DCT双域周期检测**在周期检测阶段并行运行两路检测——FFT对整周期峰位锐利，DCT借助偶对称延拓在非整数周期下泄漏更小，两路各取Top-K周期、各自完成二维折叠与卷积，从而获得比单一变换更完整的周期集合；**跨周期自相关注意力**在聚合阶段与TimesNet的二维卷积分支**并行**——卷积分支负责周期内的局部形状，自相关分支在原始时间轴上做长滞后全局聚合，二者经自适应门控融合。需要说明，频域方法用于时序预测已有大量先期工作（FEDformer、FiLM、FreTS、FITS等），本文的增量在于将FFT与DCT作为互补的**周期检测**通路，并以并行方式引入长滞后聚合。

> **[状态说明]** 本文目前处于**方法设计与实验进行中**阶段。第3节的方法描述与代码实现一致；第4节的所有数值结果**尚未产生**，均以 `[待填]` 标注，待实验完成后填入。第3.4、3.5节给出的是两个**命题（proposition）**，其定量证明尚未完成，需在投稿前补全或明确降级为设计动机（见附录A/B说明）。

**关键词**：长期预测；太阳能预测；STL分解；频域分析；自相关注意力

---

## 1 引言

### 1.1 研究背景与意义

随着全球能源转型的深入推进，太阳能作为最具发展潜力的可再生能源之一，其装机容量和发电量持续快速增长（具体统计数字与出处需在投稿前补充并核实）。然而，太阳能发电具有天然的间歇性和波动性，其出力受地理位置、季节变化、昼夜交替以及气象条件等多重因素影响，呈现出高度复杂的时间序列特性。这种特性给电网调度带来了严峻挑战：如果无法准确预测太阳能发电量，电网运营商需要在备用电源上投入额外成本以应对供需失衡；而过度保守的预测则可能导致可再生能源的浪费与弃光现象。因此，提升太阳能预测的精度和可靠性，对于降低电力系统运营成本、提高可再生能源消纳比例、推动能源绿色低碳转型具有重要的理论价值与实际意义。

太阳能预测问题在机器学习与时间序列分析领域有着悠久的研究历史。从统计方法到深度学习技术，研究者们提出了众多预测模型。然而，现有方法在实际应用中仍然面临以下核心挑战：第一，**非平稳性挑战**，太阳能时间序列的统计特性（如均值、方差、季节性强度）随时间推移发生显著变化，传统假设数据服从独立同分布的模型难以有效捕捉这种动态演化；第二，**周期检测挑战**，太阳能数据同时包含日周期、周周期与年周期等多重周期性，且受非整数周期与趋势叠加影响，单一变换的周期检测容易出现频谱泄漏与漏检；一旦检测到的周期偏离真实周期，依据其进行的二维折叠就会产生错误的相位对齐，直接影响后续建模质量；第三，**长程依赖挑战**，准确的长期预测（在本文数据的分辨率下，对应预测未来数小时至数天）需要模型具备从较长历史窗口中提取有效信息的能力，而传统循环神经网络在处理长序列时面临梯度消失与计算效率的双重瓶颈。

### 1.2 现有方法及其局限性

时间序列预测方法大致可分为三类：统计方法、传统机器学习方法和深度学习方法。

**统计方法**以ARIMA（自回归积分滑动平均模型）和Prophet为代表，通过显式建模时间序列的统计特性进行预测。ARIMA系列方法通过差分运算处理非平稳性，通过自回归和移动平均机制捕捉时间依赖。然而，ARIMA本质上是线性模型，其对太阳能数据中普遍存在的非线性交互和突变模式表达能力有限。Prophet由Facebook提出，采用加法模型将时间序列分解为趋势、季节性和假日效应三个分量，虽然对多周期建模有所改善，但其对趋势的建模仍然相对简单，且在处理高频率、高噪声的太阳能数据时性能显著下降。

**传统机器学习方法**以支持向量回归（SVR）和随机森林（RF）为代表，通过将历史数据映射到高维特征空间进行预测。这类方法能够一定程度上捕获非线性关系，但其性能高度依赖于特征工程的质量，而太阳能预测所需的特征（如太阳高度角、日照时长、气温等）往往需要领域专家知识精心设计。此外，这类方法难以端到端优化，整个预测流程被割裂为特征提取与模型训练两个独立阶段。

**深度学习方法**在过去五年间取得了突破性进展。循环神经网络（RNN）及其变体（LSTM、GRU）通过门控机制有效缓解了梯度消失问题，成为时间序列预测的主流方法。然而，RNN的顺序计算特性导致其难以并行化，训练效率低下，且仍然难以有效捕获长程依赖。Transformer架构通过全注意力机制实现了序列间依赖的并行建模，在机器翻译和自然语言处理领域取得了巨大成功。研究者们迅速将其引入时间序列预测领域，提出了Informer、Autoformer、FEDformer等一系列模型。然而，这些方法在设计时并未充分考虑时间序列的周期性归纳偏置——它们将时间序列视为普通的序列数据，忽视了昼夜交替、季节变化等固有的周期模式。

**TimesNet**通过将一维时间序列沿周期方向折叠为二维张量，再利用二维卷积提取周期内模式，在多个基准上取得优异性能。然而，从"周期建模"的完整流程来看，TimesNet仍有两处可改进之处：其一，**周期检测仅依赖FFT**。FFT以复指数为基并隐含周期延拓假设，当信号含非整数周期或边界不连续时会产生频谱泄漏，使部分周期被淹没或发生偏移，进而使折叠形状偏离真实周期结构；其二，**跨周期聚合的跨度受卷积核大小限制**。二维卷积在折叠张量上做局部聚合，核长为K时跨周期跨度约为±⌊K/2⌋个周期，对跨越数十个周期的长滞后只能逐层间接传递，缺少直接的全局聚合通路。

需要特别强调的是，TimesNet的折叠表示**本身已经编码了跨周期结构**——折叠张量的每一列即"跨周期的同相位位置"，卷积核跨行即在做跨周期聚合。因此上述第二点的准确表述是"**跨周期聚合的跨度受限、且依赖逐层堆叠**"，而非"TimesNet不具备跨周期建模能力"。本文的自相关分支正是针对这一受限之处设计的显式全局聚合通路。

### 1.3 本文贡献

针对上述挑战，本文提出深度学习框架TimesNet-Full。三个模块分别作用于周期建模流程的**三个不同阶段**——输入分解、周期检测、跨周期聚合——因此功能上互不重叠，形成"分解 → 检测 → 聚合"的流水线式结合：

**贡献一（输入分解阶段）：STL分解与三分量独立嵌入**。我们将STL风格的序列分解引入框架，把原始序列分离为趋势、季节性和残差三个分量，并为每个分量设计**独立参数**的嵌入网络。动机在于：太阳能序列中三类成分的量级与频率差异极大（趋势缓慢、季节强周期、残差高频），用同一套参数拟合会相互干扰，显式分解后由各分量自行选择表示。为保证端到端可训练与计算效率，我们采用向量化的可微实现（中心移动平均提取趋势 + 逐相位跨周期平均提取季节项），替代逐序列LOESS，使分解可批量执行且梯度可回传。

**贡献二（周期检测阶段）：FFT-DCT双域互补周期检测**。单一变换的周期检测并不完备：FFT以复指数为基并隐含周期延拓假设，在非整数周期与边界不连续处会产生频谱泄漏；DCT以实余弦为基、采用偶对称延拓，对边界不连续更鲁棒，因而能检出FFT因泄漏而淹没或偏移的周期。据此我们设计**双路并行检测**：两路各自取Top-K周期、各自完成二维折叠与卷积，再由门控自适应融合。其要点是**两条通路使用不同的周期集合**，其并集比任一单变换更完整。需要说明，频域方法用于时序预测已有大量工作（FEDformer、FiLM、FreTS、FITS等），DCT用于负荷/水文预测亦有先例；本文的增量在于将二者作为互补的**周期检测**通路并给出差异分析。

**贡献三（跨周期聚合阶段）：与TimesNet并行的自相关注意力**。TimesNet的二维卷积在折叠张量上做局部聚合，跨周期跨度受卷积核大小限制（核长K时约±⌊K/2⌋个周期），难以直接聚合长滞后。自相关注意力在**原始一维时间轴**上通过FFT计算自相关谱并选取Top-K延迟，实现跨任意长滞后的全局聚合。我们将二者设计为**并行双分支**：卷积分支负责周期内的局部形状，自相关分支负责跨周期的长滞后关联，两分支输出经自适应门控加权后与输入残差相加。

三个模块的贡献由消融实验分别评估（[待填]）。需要说明的是，本文理论部分给出的是一致性分析框架（见3.4、3.5节），用于说明"为什么这样组合是自洽的"，而非严格的最优性保证。

### 1.4 论文结构

本文其余部分组织如下：第2节介绍相关工作，包括时间序列预测方法、周期建模技术以及信号分解方法的最新进展；第3节详细介绍TimesNet-Full的模型架构、核心模块设计以及理论分析；第4节描述实验设置、主实验结果、消融研究以及可视化分析；第5节进行方法讨论，包括各模块的作用机制分析、局限性与未来方向；第6节总结全文并展望未来研究。

---

## 2 相关工作

### 2.1 深度学习时间序列预测

深度学习方法在时间序列预测领域的应用可追溯至LSTM和GRU等循环神经网络的出现。Hochreiter和Schmidhuber提出的长短期记忆网络通过引入输入门、遗忘门和输出门机制，有效解决了传统RNN的梯度消失问题，使得模型能够学习长距离时间依赖。GRU作为LSTM的简化变体，在保持相近表达能力的同时降低了计算复杂度。然而，RNN的本质是顺序计算——当前时刻的隐藏状态依赖于前一时刻的隐藏状态——这导致两个根本性限制：一是无法并行计算，训练效率受限于序列长度；二是对长程依赖的建模仍然不够充分，尽管门控机制有所改善。

Transformer架构的出现彻底改变了序列建模的游戏规则。Vaswani等人提出的自注意力机制允许序列中任意位置直接与所有其他位置进行信息交互，突破了RNN的顺序依赖限制。基于Transformer的时间序列预测模型迅速涌现。Informer针对标准Transformer在长序列上的计算复杂度问题，提出了ProbSparse自注意力和蒸馏机制，将复杂度从O(L²)降低至O(L log L)。Autoformer引入了自相关注意力机制，通过计算序列与滞后版本的相关性来建模周期依赖，并在解码器端设计了序列分解架构。FEDformer基于傅里叶和小波变换，在频域中进行注意力计算，进一步提升了长序列处理能力。PatchTST提出了基于Patch的分词策略，将时间序列划分为重叠的片段作为Transformer的输入token，在长期预测任务上取得了优异性能。

然而，上述方法在设计时普遍将时间序列视为普通的序列数据，忽视了时间序列特有的周期性归纳偏置。现实世界的时间序列——尤其是太阳能发电数据——其统计特性往往呈现出规律性的周期波动。忽略这一特性意味着模型需要从大量数据中"重新学习"本可显式建模的周期模式，既浪费了计算资源，也限制了模型性能的天花板。

### 2.2 周期建模技术

周期建模是时间序列分析的核心问题之一。传统方法主要依赖傅里叶分析。快速傅里叶变换（FFT）通过将时域信号转换到频域，能够有效识别信号中的主导频率成分。傅里叶变换的理论基础在于，任何周期函数都可以表示为一系列正弦和余弦函数的线性组合。在时间序列预测中，FFT被广泛用于识别数据中的季节性周期，指导模型选择合适的时间窗口和分解策略。Wu等人提出的FEDformer即基于傅里叶变换，在频域中进行特征提取和注意力计算。

然而，FFT并非万能。FFT将信号视为周期延拓，当信号含非整数周期或存在边界不连续时，频谱会出现泄漏，导致周期估计偏移或漏检。离散余弦变换（DCT）提供了另一种选择：它以实数余弦函数为基，等价于对信号做**偶对称延拓**，避免了FFT在边界处强行首尾拼接所带来的不连续，因而在存在非整数周期时频谱更集中、泄漏更小。需要强调的是，DCT在本文中的作用是**提供一组与FFT互补的周期检测结果**，而不是替代序列分解模块去处理趋势——趋势的分离由本文的STL模块负责（见2.3节与3.3节），两者分工不同、作用阶段也不同。我们将在第3.4节给出FFT与DCT在周期检测上的差异分析。

在深度学习框架中，周期建模的一个重要突破是TimesNet。TimesNet的核心思想是将一维时间序列沿周期方向"折叠"为二维张量，然后利用二维卷积神经网络进行特征提取。具体而言，对于周期为p的序列x ∈ ℝᵀ，可以将其reshape为二维张量X ∈ ℝ^(T/p)×p，使得每个行向量代表一个完整周期，每个列向量代表周期内同一时刻的不同周期实例。这种表示使得卷积核能够自然地捕获周期内的局部模式（如每天同一时刻的用电规律）。需要指出，**该折叠表示本身已经编码了跨周期结构**（每一列即跨周期的同相位位置）；TimesNet的局限在于卷积核大小限制了跨周期感受野，而非完全不具备跨周期建模能力。

**频域方法的既有工作**。需要承认，频域建模在时间序列预测中已相当成熟：FEDformer（ICML 2022）将傅里叶与小波变换嵌入注意力，FourierGNN（AAAI 2024）在图结构上做傅里叶变换，FiLM（NeurIPS 2022）使用频率增强的Legendre记忆，FreTS（NeurIPS 2023）直接在频域用MLP建模，FITS（ICLR 2024）用频率插值以极少参数完成预测。此外，Zeng等（AAAI 2023）以DLinear等线性基线质疑了复杂模型的必要性，FilterNet（NeurIPS 2024）进一步论证"简单滤波器已足够"。**因此，本文的双域设计必须在与这些简单强基线的对比下证明其价值，而不能仅凭"引入DCT"作为新颖性。**

### 2.3 信号分解方法

信号分解是将复杂信号拆解为若干简单分量之和的技术，是处理非平稳时间序列的重要手段。最经典的分解方法是1951年Wold提出的Wold分解定理，该定理指出任何平稳时间序列都可以分解为确定性的移动平均分量和随机性的AR分量。Box-Cox变换通过非线性变换使非平稳序列趋于平稳，广泛用于方差稳定化。

季节性趋势分解（STL）是 Cleveland等人于1990年提出的经典方法，通过局部加权回归（LOESS）将时间序列分解为趋势、季节性和残差三个分量。STL的核心优势在于其稳健性——LOESS对异常值不敏感，且允许季节性强度随时间变化。相比传统的季节性分解（如X-13ARIMA-SEATS），STL对异常值和缺失值更具鲁棒性，且计算过程透明可解释。然而，原始STL需要预先指定单一固定周期，这对于同时包含多周期模式的数据（如太阳能数据同时具有日周期和年周期）而言是明显的局限。

近年来，深度学习与信号分解的结合成为研究热点。Informer和Autoformer在模型架构中引入了显式的序列分解模块。Autoformer在编码器和解码器的每个层之间插入分解层，将隐表示分离为趋势项和周期项，对两部分分别进行处理后再融合。FEDformer将傅里叶变换和小波变换嵌入注意力机制，在频域中直接操作季节性和趋势分量。这些方法通常只进行单次或单一尺度的分解。

> **[本文范围说明]** 上述"单一固定周期"与"单一尺度"的局限用于说明分解类方法的设计空间，**并不意味着本文已解决该问题**——本文的STL同样是单周期的（两个数据集分别取日周期288与96步）。多周期/多尺度分解留作未来工作（见5.4节）。此处明确这一点，以避免读者误认为本文的分解模块解决了多周期问题。

### 2.4 自相关注意力机制

自相关注意力（Auto-Correlation Attention）由Wu等人于2021年在Autoformer中首次提出，旨在解决标准注意力机制忽视时间序列周期性的问题。核心思想是：时间序列的当前时刻往往与若干历史时刻存在相似性，这种相似性（自相关）随滞后时间的变化呈现一定的规律性。例如，太阳能发电量在"今天中午"与"昨天中午"可能高度相关，而"今天中午"与"今天凌晨"则相关性很低。自相关注意力通过计算序列x与滞后版本x_(t-τ)的互相关来建模这种关系，然后根据相关强度对历史信息进行加权聚合。

与标准注意力的Query-Key-Value交互不同，自相关注意力直接在Value上操作：通过识别序列中与当前时刻高相关的历史时刻，将这些历史信息"搬运"到当前位置。这种机制天然符合时间序列的周期性规律——它能够自动学习并利用"同周期同相位"的时间依赖，而无需人工设计特征或先验知识。Autoformer报告称该机制在其评测中优于标准注意力。

然而，自相关注意力作为Autoformer的一部分，通常与该模型的编码器-解码器架构紧密耦合；且后续研究表明，Autoformer的增益主要来自序列分解与逐序列注意力，自相关本身并非关键因素。**本文的做法**是将自相关注意力引入TimesNet框架，与二维卷积并行组合。具体而言，TimesNet的卷积在折叠后的二维张量上操作，其跨周期感受野受卷积核大小限制；自相关注意力在原始一维序列上操作，通过自相关谱选择Top-K延迟，可覆盖更长的滞后。两者的互补性体现在**感受野**上（而非"正交操作域"，该说法不成立）。该设计的收益需由消融实验确认。

---

## 3 方法论

### 3.1 问题定义

本文研究太阳能长期预测问题，其形式化定义如下：给定历史观测序列x = (x₁, x₂, ..., x_T) ∈ ℝ^T，其中T为输入序列长度，目标是预测未来H步的数值：

$$\hat{x}_{T+1}, \hat{x}_{T+2}, ..., \hat{x}_{T+H} = \arg\max_{\theta} \ \mathcal{L}(x_{T+1:T+H}, f_\theta(x_{1:T}))$$

其中f_θ(·)为可学习的预测函数，𝓛为损失函数（本文使用均方误差MSE）。对于多变量太阳能预测，输入和输出均为多维向量，即x_t ∈ ℝ^C，C为变量数量（本文两个数据集分别为10与8）。

长期预测相比短期预测更具挑战性，原因有三：第一，长期预测需要模型具备更强的长程依赖建模能力；第二，预测误差会随步长累积，导致长期预测的精度天花板更低；第三，太阳能数据的季节性和周期性在较长跨度上更为显著，有效利用周期信息至关重要。（注：本文两个数据集分辨率不同，同一 pred_len 对应的物理时长不同，因此"长期"以物理时长而非步数衡量，详见4.3节。）

### 3.2 整体架构

TimesNet-Full的整体架构遵循编码器-预测器范式。三个模块分别对应周期建模流程的**三个阶段**，构成"**分解 → 检测 → 聚合**"的流水线：

- **输入分解阶段**（3.3节）：STL分解器将输入序列拆为趋势/季节/残差，三分量各经独立嵌入后相加，得到编码表示；
- **周期检测阶段**（3.4节）：双域周期检测在编码表示上并行执行FFT与DCT两路检测，各自完成二维折叠与卷积后门控融合；
- **跨周期聚合阶段**（3.5节）：自相关注意力分支与上述卷积分支并行，输出门控融合后再做残差与归一化。

整体数据流如图1所示。

**图1：TimesNet-Full整体架构图**

```
输入序列 x₁:T
     ↓
┌────────────────────────────────┐
│   STL分解（周期p）      ← 阶段1 │
│   趋势 / 季节 / 残差             │
└────────────────────────────────┘
     ↓
┌────────────────────────────────┐
│   三分量独立嵌入 → 相加          │
└────────────────────────────────┘
     ↓  编码表示 E ∈ ℝ^(T×d)
┌────────────────────────────────┐
│   DualDomainTimesBlock × N      │
│   ├ 分支A：双域周期卷积  ← 阶段2 │
│   └ 分支B：自相关注意力  ← 阶段3 │
│        → 门控融合 → LayerNorm    │
└────────────────────────────────┘
     ↓  隐表示 H ∈ ℝ^(T×d)
┌────────────────────────────────┐
│   预测头 Projection             │
└────────────────────────────────┘
     ↓
预测序列 ŷ_{T+1:T+H} ∈ ℝ^(H×C)
```

主干网络由N个DualDomainTimesBlock堆叠而成（N默认为2）。每一层包含两个**并行**分支：**FFT-DCT双域卷积分支**与**跨周期自相关注意力分支**，两者的输出经自适应门控加权融合后与输入残差相加，再经LayerNorm。卷积分支侧重周期内的局部模式，注意力分支侧重跨周期的长滞后依赖；门控权重由训练自动学习。需要明确，两个分支在**每一层**都并行存在，而非"前层卷积、后层注意力"的串行堆叠。

### 3.3 STL分解模块

太阳能时间序列的非平稳性体现在：短期波动（小时级）、中期季节性（日级）和长期趋势（年月级）的叠加。为有效分离这些成分，我们采用STL风格的序列分解方法。

**分解原理**。STL（Seasonal-Trend decomposition using Loess）由Cleveland等人于1990年提出，将时间序列分解为趋势、季节性和残差三个分量：

$$x_t = \text{Trend}_t + \text{Seasonal}_t + \text{Residual}_t$$

经典STL使用局部加权回归（LOESS）估计趋势与季节项，对异常值鲁棒，但需要对每条序列单独迭代求解，无法批量并行，且不可微分。为嵌入端到端训练流程，本文采用其**向量化可微近似**：（1）趋势项由中心移动平均估计（序列两端做复制填充以避免边界偏差）；（2）季节项由去趋势序列在周期内的**逐相位跨周期平均**估计；（3）残差为三者之差。为降低分量间的相关性，采用两遍估计（先去趋势求季节项，再对去季节序列重新估计趋势），对应经典STL的内循环思想。该实现完全向量化、可在GPU上批量执行，且梯度可回传至嵌入网络。

分解周期p以**步数**为单位，需与数据采样间隔匹配：Solar-DKA（5分钟采样）的日周期为 p=288，Solar-Site1（15分钟采样）的日周期为 p=96。趋势窗口取经典STL默认值，即不小于 1.5p/(1−1.5/s) 的最小奇数（s为季节平滑窗口，默认13）。

> **[关键约束：T ≥ 2p]** 季节项由"逐相位跨周期平均"估计，至少需要2个完整周期才有意义；若 T < 2p，季节项退化为0（代码中已作该保护），STL模块将**失效**。这一约束直接影响实验设置：**若 Solar-DKA 沿用 seq_len=96，则 96 < 2×288，季节项恒为0，贡献一在该数据集上不起作用**。因此在 Solar-DKA 上必须令 seq_len ≥ 576（覆盖2个日周期），或降低分解周期（如取半天周期 p=144，则需 seq_len ≥ 288）。**该设置必须在实验前确定并写入4.3节**——这是方法能否在DKA上生效的前提，而非可选项。

**深度学习框架中的集成**。不同于传统方法将STL作为独立的预处理步骤，我们将其嵌入神经网络流程。给定批量输入 x ∈ ℝ^(B×T×C)，对批量内所有 (样本, 变量) 对**并行**执行分解（向量化实现，非逐序列循环）：

$$[\text{trend}, \text{seasonal}, \text{residual}] = \text{STL}(x,\ \text{period}=p)$$

三个分量通过**独立的嵌入网络**进行特征提取：

$$E_{\text{trend}} = \text{Embed}_{\text{trend}}(\text{trend})$$
$$E_{\text{seasonal}} = \text{Embed}_{\text{seasonal}}(\text{seasonal})$$
$$E_{\text{residual}} = \text{Embed}_{\text{residual}}(\text{residual})$$

最终的嵌入表示为三者之和：

$$E = E_{\text{trend}} + E_{\text{seasonal}} + E_{\text{residual}}$$

三个分量各自经过**独立参数**的嵌入网络（而非共享权重），使模型能够针对不同分量的特性进行差异化处理——趋势分量捕获长期变化，季节分量捕获周期模式，残差分量捕获随机波动。

**计算效率考虑**。若直接使用statsmodels的LOESS实现，分解需对每个(样本,变量)对单独执行：批量大小B、变量数C时，每前向需 B×C 次拟合，在CPU上会产生数十秒的单步开销，成为训练瓶颈。本文的向量化实现将该开销降至毫秒级（实测 B=32、T=96、C=10 时约0.03秒，且可在GPU上完成）。代码保留 `--stl_impl statsmodels` 选项，用于与经典STL的数值对照。

### 3.4 FFT-DCT双域周期检测

周期检测是TimesNet类方法的关键步骤——周期估计决定二维折叠的形状，直接影响后续卷积的效果。TimesNet仅使用FFT进行周期检测，本文认为这一环节存在可改进之处。

**快速傅里叶变换（FFT）**通过复指数函数基展开将信号转换到频域。对于长度为T的序列x，其FFT定义为：

$$\hat{x}[k] = \sum_{t=0}^{T-1} x[t] \cdot e^{-2\pi i k t / T}, \quad k = 0, 1, ..., T-1$$

FFT的频谱能量分布揭示了信号的周期结构：频谱峰值对应的频率 f_k = k/T 的倒数即为检测到的周期。FFT对严格周期性信号（如正弦波）的频率定位精确、峰值锐利。但FFT隐含"信号是周期延拓"的假设：当真实周期不能整除窗口长度T（即非整数周期），或信号首尾不连续时，能量会从真实频率向相邻频点扩散，即**频谱泄漏**。泄漏会使非主导周期被淹没，或使峰位发生偏移，从而得到偏离真实的周期估计。

**离散余弦变换（DCT）**使用实数余弦函数作为基函数。DCT-II的定义如下：

$$\hat{x}[k] = \sum_{t=0}^{T-1} x[t] \cdot \cos\left(\frac{\pi k (t + 0.5)}{T}\right), \quad k = 0, 1, ..., T-1$$

DCT的关键性质在于它等价于对信号做**偶对称延拓**（边界处镜像），因此不存在FFT那样"首尾强行拼接"引入的不连续。对于含非整数周期的信号，偶对称延拓使频谱能量更集中、泄漏更小，因而DCT能够检出FFT因泄漏而淹没或偏移的部分周期。需要明确：**DCT在本文中用于周期检测，不承担趋势分离的职责**——趋势由3.3节的分解模块显式提取，两者作用阶段不同。

**命题1（FFT与DCT的周期检测互补性，待证明）**：设 x(t) = Trend(t) + ΣₖAₖsin(ωₖt) + ε(t)，其中Trend为低阶多项式趋势项，ΣₖAₖsin(ωₖt)为K个周期分量，ε为白噪声。则：

1. 当周期不能整除窗口长度时（2π/ωₖ ∤ T），FFT频谱发生泄漏，其Top-K峰位与真实周期集合之间存在系统性偏差；
2. DCT因采用偶对称延拓，在同一情形下泄漏更小，其Top-K峰位与真实周期集合的偏差小于FFT；
3. 因此两者检测到的周期集合不完全重合，其**并集**比任一单变换更接近真实周期集合。

> **[待完成]** 需给出"泄漏量"与"峰位偏差"的定量刻画（例如用Dirichlet核的旁瓣衰减表达）。附录A尚未完成，**投稿前必须补全，或降级为设计动机并删除"命题"提法**。避免使用"正交性""定理"等过强表述。注意第3点正是架构所依赖的性质——双路各自使用自己的周期集合，其并集由门控融合——因此命题与架构是自洽的。

基于上述分析，我们设计了**FFT-DCT双域周期检测**模块，完整流程如下：

**第一步：双域频谱计算**。对输入序列x ∈ ℝ^(B×T×C)分别计算FFT和DCT：

$$\text{FFT\_amp}[k] = \frac{1}{BC} \sum_{b=1}^{B} \sum_{c=1}^{C} |\hat{x}_{bc}[k]|, \quad \text{DCT\_amp}[k] = \frac{1}{BC} \sum_{b=1}^{B} \sum_{c=1}^{C} |\tilde{x}_{bc}[k]|$$

**第二步：Top-K周期提取**。分别从FFT和DCT频谱中提取能量最高的K个频率，取其倒数为周期：

$$\text{FFT\_periods} = T / \arg\top_K(\text{FFT\_amp}[1:])$$

$$\text{DCT\_periods} = T / \arg\top_K(\text{DCT\_amp}[1:])$$

其中索引从1开始以去除直流分量（频率为0）。FFT与DCT通常提取出不同但互补的周期集合：FFT对能整除T的整周期峰位锐利，DCT则能检出因泄漏在FFT频谱中偏移或被淹没的周期。**两条通路各取各的周期集合并各自完成折叠与卷积，是本文"双域互补"的具体落点。**

**第三步：周期权重计算**。每个周期的权重由其在对应频域中的能量归一化得到：

$$\text{FFT\_weights}[i] = \frac{\text{FFT\_amp}[f_i]}{\sum_{j=1}^{K} \text{FFT\_amp}[f_j]}$$

$$\text{DCT\_weights}[i] = \frac{\text{DCT\_amp}[f_i]}{\sum_{j=1}^{K} \text{DCT\_amp}[f_j]}$$

**第四步：双路2D卷积处理**。对每个检测到的周期p，将序列沿时间维折叠为 ⌈L/p⌉×p 的二维张量（L为对齐后的时间长度，不足一个周期时在末尾补零），分别通过Inception卷积块提取特征。FFT和DCT两路并行处理，输出特征维度相同。

**第五步：自适应门控融合**。融合门控由双路特征的拼接经两层MLP得到：

$$\text{gate} = \text{softmax}(\mathbf{W}_2 \cdot \tanh(\mathbf{W}_1 \cdot [\text{FFT\_out}; \text{DCT\_out}] + \mathbf{b}_1) + \mathbf{b}_2)$$

最终输出为双路特征的加权融合：

$$\text{output} = \text{gate}[0] \cdot \text{FFT\_out} + \text{gate}[1] \cdot \text{DCT\_out}$$

### 3.5 DualDomainTimesBlock

DualDomainTimesBlock是主干网络的重复单元。每一层内部包含两个**并行分支**：双域周期卷积分支与自相关注意力分支；两分支输出经自适应门控加权融合后与输入残差相加，最后经LayerNorm。结构如图2所示。

**图2：DualDomainTimesBlock结构图**

```
输入 X ∈ ℝ^(B×T×d)
   │
   ├── 分支A：双域周期卷积分支 ──────────────────────────┐
   │      FFT周期检测 → 2D折叠 + Inception ─┐            │
   │      DCT周期检测 → 2D折叠 + Inception ─┴→ 门控融合 ─┤
   │                                                    ├→ 门控融合 → +X → LayerNorm → 输出
   └── 分支B：自相关注意力分支 ───────────────────────────┘
          自相关谱(FFT加速) → Top-K延迟 → 延迟聚合
```

**双域卷积分支**。给定周期p，特征沿时间维折叠为 ℝ^(B×d×⌈L/p⌉×p) 的四维张量（L为对齐后的时间长度，末尾补零至p的整数倍），再由Inception块处理。Inception块包含多个并行二维卷积分支（`num_kernels=6`，核大小为1、3、5、7、9、11），以捕获不同尺度的周期内模式。FFT路径与DCT路径结构相同，但**使用各自检测到的周期集合，且参数不共享**。

**自相关注意力分支**。该分支与卷积分支**并行**（并非"仅第二层叠加"）。计算分三步：

（1）**自相关计算**：取 Q = K = V = X（自回归设置），计算Query与Key的互相关谱：

$$\text{corr}[\tau] = \frac{1}{T} \sum_{t=1}^{T} Q[t] \cdot K[t - \tau]$$

使用FFT加速：corr = IFFT(FFT(Q) · conj(FFT(K)))。

（2）**Top-K延迟选择**：找出相关强度最高的K个延迟τ₁, τ₂, ..., τ_K：

$$\tau_1, ..., \tau_K = \arg\top_K(\text{corr})$$

（3）**延迟聚合**：将历史信息沿Top-K延迟聚合到当前位置：

$$\text{attn}[t] = \sum_{k=1}^{K} w_k \cdot V[t - \tau_k]$$

其中权重w_k = softmax(corr[τ_k])。

**命题2（两分支的功能互补性，待严格化）**：设X ∈ ℝ^(B×T×d)为输入，CNN(X)为双域卷积分支输出，Attn(X)为自相关分支输出。则：

1. **操作轴不同**：CNN在折叠后的二维张量（形状 T/p × p）上操作，Attn在原始一维时间轴（长度T）上操作；
2. **聚合范围不同**：CNN的聚合范围由卷积核大小决定，核长为K时跨周期跨度约为±⌊K/2⌋个周期，长滞后只能逐层间接传递；Attn通过Top-K延迟选择可覆盖任意滞后，包括远长于卷积跨度的滞后；
3. **对周期检测准确性的依赖不同**：CNN依赖折叠带来的相位对齐（周期检测偏差会直接导致错位），Attn不依赖显式折叠，而是从自相关谱中选取延迟。

> **[待完成]** 原文曾称两分支"操作域正交、Jacobian为外积、梯度独立"——这些表述**在数学上不成立**（复合映射的Jacobian是乘积而非外积，非线性模块的梯度也无可证的正交性），已删除。上述三条是**可验证的结构性差异**，应以此为准并放弃"正交性定理"的提法。另需注意：本实现中Top-K延迟**跨batch共享**（沿用Autoformer的加速设计），故第3条中"选取延迟"指全局延迟偏好，而非逐样本注意力。

### 3.6 训练目标与优化

TimesNet-Full采用标准MSE损失函数进行端到端训练：

$$\mathcal{L} = \frac{1}{B \cdot H \cdot C} \sum_{b=1}^{B} \sum_{t=1}^{H} \sum_{c=1}^{C} (y_{btc} - \hat{y}_{btc})^2$$

其中y为真实值，ŷ为预测值。优化器为 **Adam**（初始学习率 1e-3），学习率按 `type1` 策略调整——**每个epoch减半**。训练采用早停机制（耐心值3个epoch）。

> **[与代码一致性说明]** 原稿曾声称"AdamW + 余弦退火 + 3000步预热 + 混合精度"。经核对 `exp/exp_long_term_forecasting.py`，**实际使用的是 Adam + 每轮减半的阶梯衰减，且没有预热**；混合精度（`--use_amp`）在代码中默认**关闭**。上述描述已按代码更正。若希望改用余弦退火，可通过 `--lradj cosine` 切换，届时需在文中写明实际所用策略。

---

## 4 实验

### 4.1 数据集描述

本文使用两个真实太阳能数据集，均位于仓库的 `data_provider/` 目录下，由 `Dataset_Solar` 统一加载。

**Solar-DKA**（`DKA Solar Center dataset.csv`）。数据来自澳大利亚达尔文市的DKA Solar Centre，采样间隔**5分钟**，时间跨度**2014-01-01 至 2015-11-30（约1.9年）**，共192,375条记录。包含**10个数值列**：发电功率 `Active_Power`（预测目标）以及9个气象变量（风速、气温、相对湿度、水平总辐射、水平散射辐射、风向、日降雨量、倾角总辐射、倾角散射辐射）。

**Solar-Site1**（`site1.xlsx`）。采样间隔**15分钟**，时间跨度**2023-01-01 至 2023-12-31（整1年）**，共35,040条记录，无缺失值。包含**8个数值列**：实际发电功率（预测目标，`实际发电功率(mw)`）以及7个气象变量（组件温度、环境温度、气压、湿度、总辐射、直射辐射、散射辐射）。该数据集的列名均为**中文**（时间列为`时间`），加载器已做列名与时间列的自适应识别。

两个数据集共同具备的建模特点：

1. **强日周期**——昼夜交替使发电功率呈现显著日内波动。**周期长度取决于采样分辨率**：5分钟采样下日周期为288步，15分钟采样下为96步。这一差异使两个数据集天然构成"同一现象、不同时间尺度"的对照；
2. **夜间零值**——发电功率在夜间恒为0，MSE/MAE会被低值区段主导，需配合MAPE等相对指标并分段报告；
3. **非平稳性**——季节强度随月份变化，并存在长期趋势。

> **[数据校验说明]** 原稿曾将第一个数据集描述为"137个光伏电站、1小时采样、10年（2012–2022）"。经核对原始文件，实际为**10个变量、5分钟采样、约1.9年、单一站点**。原稿中一切涉及"137站点/空间相关性/多电站"的论述均已删除。

所有数据集按时间顺序划分：训练集70%、验证集10%、测试集20%，严格遵循时间顺序、不随机打乱（与 `Dataset_Solar.__read_data__` 一致）。

### 4.2 基准模型对比

我们将TimesNet-Full与若干基线在**两个数据集（Solar-DKA、Solar-Site1）**上对比。可比基线限于可在 `run.py` 中直接运行的模型（TimesNet、DLinear、PatchTST、Autoformer、FEDformer、Informer 等，取决于本版本 `models/__init__.py` 的注册情况）；ARIMA、LSTM、TCN 等需另行实现。原稿列出"12个基准 × 4个数据集"，其中 Electricity/ETTh1/Weather 未接入、部分基线未实现，**实际可比数量需按仓库现状重新统计后写入**。

基线选择遵循两条原则：（1）必须包含 **DLinear / PatchTST** 这类简单强基线——已有工作（Zeng et al., AAAI 2023）表明它们在多个基准上可匹敌甚至超过复杂模型，回避它们会削弱结论可信度；（2）必须包含**容量对齐**的 TimesNet（见4.8节），以排除"增益来自参数变多"的解释。

### 4.3 实验设置

**预测任务设置**。输入长度 seq_len ∈ {96, 192, 336, 720}，预测长度 pred_len ∈ {96, 192, 336, 720}。**步长与物理时长的换算随数据集分辨率而不同**：

| pred_len | Solar-DKA（5分钟采样） | Solar-Site1（15分钟采样） |
|---|---|---|
| 96 | 8 小时 | 24 小时（1天） |
| 192 | 16 小时 | 48 小时（2天） |
| 336 | 28 小时 | 84 小时（3.5天） |
| 720 | 60 小时（2.5天） | 180 小时（7.5天） |

原稿"pred_len=720 对应 1–4 周"是按小时采样写的，与两个数据集的真实分辨率均不符。特别注意：Solar-Site1 的 15 分钟分辨率下 pred_len=96 恰好是**一个完整日周期**，而 Solar-DKA 的 5 分钟分辨率下 96 步仅 8 小时——**同一 pred_len 在两个数据集上物理含义不同**，因此报告中须同时给出物理时长。

由此产生两个必须分别设定的参数（不能两个数据集共用一套）：

1. **STL分解周期**：DKA 为 **288**（日周期），Site1 为 **96**；
2. **seq_len**：由3.3节的 T ≥ 2p 约束，DKA 的 seq_len 必须 **≥ 576**，Site1 需 **≥ 192**。**若在 DKA 上沿用 seq_len=96，STL 季节项会恒为0**，贡献一在该数据集上完全失效——这一点必须在实验设置中显式说明。

**模型配置**。TimesNet-Full的默认配置：d_model=64，d_ff=64，e_layers=2，num_kernels=6，周期检测Top-K=5，STL分解周期 `--stl_period`（**以步数为单位**，需满足 T ≥ 2p，见3.3节）。batch size 默认32（`--batch_size`）。

> **[与代码一致性说明]** 原稿称"所有参数使用Xavier初始化、训练时梯度裁剪（最大范数1.0）"。经核对，**Inception卷积块的权重使用 Kaiming 初始化**（`Inception_Block_V1._initialize_weights`），其余层为 PyTorch 默认初始化；训练循环中**没有梯度裁剪**。已按代码更正。

**评估指标**。主要指标为MSE与MAE；由于夜间发电功率恒为0，额外报告MAPE：

$$\text{MAPE} = \frac{100\%}{N} \sum_{i=1}^{N} \left| \frac{y_i - \hat{y}_i}{y_i} \right|$$

**零值处理方式必须明确写出**：MAPE在 y_i = 0 时无定义，需说明采用哪种处理（排除 y_i < ε 的时段，或改用 sMAPE），否则结果不可复现。此外应**分别报告白天与夜间的MSE**，避免整体MSE被夜间恒零区段主导而掩盖白天的真实差异。

### 4.4 主实验结果

**表1：Solar-DKA数据集长期预测结果（数值待填）**

> 下表仅给出实验设计（模型 × 预测长度）；所有单元格为 `[待填]`。基线模型需先通过 `run.py` 在本仓库跑通，TimesNet-Full 由 `train_solar_full.py` 产生。禁止在未运行的情况下填入任何数值。

| Model | MSE↓ | MAE↓ | MAPE↓ |
|-------|------|------|-------|
| | *pred_len=96* | | |
| TimesNet（基线） | [待填] | [待填] | [待填] |
| TimesNet-Full | [待填] | [待填] | [待填] |
| DLinear | [待填] | [待填] | [待填] |
| PatchTST | [待填] | [待填] | [待填] |
| Autoformer | [待填] | [待填] | [待填] |
| FEDformer | [待填] | [待填] | [待填] |
| Informer | [待填] | [待填] | [待填] |
| | *pred_len=192* | | |
| TimesNet（基线） | [待填] | [待填] | [待填] |
| TimesNet-Full | [待填] | [待填] | [待填] |
| | *pred_len=336* | | |
| TimesNet（基线） | [待填] | [待填] | [待填] |
| TimesNet-Full | [待填] | [待填] | [待填] |
| | *pred_len=720* | | |
| TimesNet（基线） | [待填] | [待填] | [待填] |
| TimesNet-Full | [待填] | [待填] | [待填] |

表1的**结论段落将在数值产出后撰写**。撰写时需注意：Solar-DKA夜间功率恒为0，MSE会被夜间区段主导，因此除MSE/MAE外应同时报告MAPE（并说明零值处理方式），否则不同模型的比较可能失真。

**表2：Solar-Site1数据集长期预测结果（数值待填）**

> Solar-Site1为15分钟采样，pred_len=96即**一整个日周期**，因此该数据集上的设置具有明确的物理解释。全部为 `[待填]`。

| Model | MSE↓ | MAE↓ | MAPE↓ |
|-------|------|------|-------|
| | *pred_len=96（=1天）* | | |
| TimesNet（基线） | [待填] | [待填] | [待填] |
| TimesNet-Full | [待填] | [待填] | [待填] |
| DLinear | [待填] | [待填] | [待填] |
| PatchTST | [待填] | [待填] | [待填] |
| | *pred_len=192（=2天）* | | |
| TimesNet（基线） | [待填] | [待填] | [待填] |
| TimesNet-Full | [待填] | [待填] | [待填] |
| | *pred_len=336（=3.5天）* | | |
| TimesNet（基线） | [待填] | [待填] | [待填] |
| TimesNet-Full | [待填] | [待填] | [待填] |
| | *pred_len=720（=7.5天）* | | |
| TimesNet（基线） | [待填] | [待填] | [待填] |
| TimesNet-Full | [待填] | [待填] | [待填] |

> **[跨数据集分析计划]** 两个数据集采样分辨率不同（5分钟 vs 15分钟），同一方法在不同周期尺度下的表现差异本身就是可报告的结论。若双域周期检测的增益在某一分辨率下显著、另一分辨率下不明显，需结合"真实周期能否整除窗口长度"来解释——这正是命题1所预测的现象，可作为对该命题的实证检验。

**图3：预测长度 vs MSE 曲线**（待生成）。计划绘制各模型在两个数据集上MSE随pred_len的变化。原稿中"TimesNet-Full上升曲线最平缓""优势在pred_len≥336时进一步扩大"等结论**均为无来源的编造，已删除**——曲线形状须由实测数据决定。

### 4.5 消融研究

为验证各模块的独立贡献，我们设计了完整的消融实验。

**表3：消融实验结果（两个数据集，pred_len=[待定]，数值待填）**

> 消融应在 Solar-DKA 与 Solar-Site1 **两个数据集上分别进行**，因为两者的采样分辨率不同（5分钟 vs 15分钟），周期检测的难易程度不同。

> 三个开关在代码中分别由 `--use_stl`、`--use_dct`、`--use_autocorr` 控制——**DCT分支已可通过 `--use_dct 0` 单独关闭**，因此"仅FFT vs FFT+DCT"这一行可直接运行。注意：关闭DCT会使参数量近乎减半（见4.8节实测），报告该行时**必须同时注明参数量差异**，否则无法区分"双域带来的信息增益"与"参数变多带来的容量增益"。

> 表中"TimesNet（基线）"一行可由 TimesNet-Full 同时设置 `--use_stl 0 --use_dct 0 --use_autocorr 0` 得到（此时退化为：普通嵌入 + 仅FFT单域卷积 + 无自相关分支），结构上与原始 TimesNet 等价。但**仍建议同时报告官方 TimesNet 实现的结果**，以排除实现差异带来的偏差。

| 配置 | STL | FFT+DCT | AutoCorr | MSE | MAE | MAPE |
|------|-----|---------|----------|-----|-----|------|
| TimesNet（基线） | ❌ | ❌ | ❌ | [待填] | [待填] | [待填] |
| + STL | ✅ | ❌ | ❌ | [待填] | [待填] | [待填] |
| + FFT+DCT | ❌ | ✅ | ❌ | [待填] | [待填] | [待填] |
| + AutoCorr | ❌ | ❌ | ✅ | [待填] | [待填] | [待填] |
| **Full（全部）** | ✅ | ✅ | ✅ | [待填] | [待填] | [待填] |

消融结论将在数值产出后撰写。撰写时应避免把各模块增益简单相加来论证"协同"——单模块增益不具可加性，需通过两两组合的实测值来判断交互效应。

### 4.6 周期检测分析

**表4：FFT与DCT检测到的周期（待填）**

> 周期以**步数**为单位（5分钟采样下，288步=1天）。可用 `dual_domain_period_detection` 直接导出并按步数→物理时间换算填入。原稿中的"24.0h/168.0h"是按小时采样假设写的，与当前数据分辨率不符，必须重算。

| 排名 | FFT检测周期(步) | DCT检测周期(步) | 对应物理周期 |
|------|---------------|----------------|-------------|
| 1 | [待填] | [待填] | [待填] |
| 2 | [待填] | [待填] | [待填] |
| 3 | [待填] | [待填] | [待填] |
| 4 | [待填] | [待填] | [待填] |
| 5 | [待填] | [待填] | [待填] |

周期列表产出后，需**实际检验**"FFT与DCT检测到互补周期集合"这一论断是否成立——这是全篇最关键的实证点：若两路周期集合高度重合，则"双域互补"的立论不成立。

> **[已与代码对齐]** DCT分支现已实现为**标准DCT-II**（`dct_ii()`，半采样偏移的余弦基 D[k,t]=cos(πk(2t+1)/2T)，基矩阵按序列长度缓存），与3.4节的公式一致。此前版本用 `cat([x, x.flip()])` + rfft 实现，那等价于DCT-I、与公式不符，该实现已被替换。

### 4.7 AutoCorrelation可视化

**图4：自相关谱/注意力权重热图（待生成）**

计划绘制自相关谱 `corr[τ]` 随滞后τ的分布，检验模型是否在τ≈日周期、周周期处出现峰值。需注意：本实现中Top-K延迟是**跨batch共享**的（沿用Autoformer的加速设计），因此"注意力热图"反映的是平均延迟偏好而非逐样本注意力，绘图时需说明这一点。

### 4.8 计算效率分析

**表5：计算效率对比（数值待填）**

| Model | FLOPs (G) | Params (M) | Latency (ms/batch) |
|-------|-----------|------------|-------------------|
| TimesNet | [待填] | [待填] | [待填] |
| TimesNet-Full | [待填] | [待填] | [待填] |
| Autoformer | [待填] | [待填] | [待填] |

参数量的估算可在本仓库直接测得。**实测（d_model=64, d_ff=64, e_layers=2）**：TimesNet-Full（双域开启）约 **9.47M**，关闭DCT分支（仅FFT）约 **4.76M**；该值对两个数据集几乎相同（Solar-DKA enc_in=10：9.468M/4.764M；Solar-Site1 enc_in=8：9.466M/4.762M），因为参数量由 Inception 卷积块主导，对输入列数不敏感。原稿称 TimesNet-Full 为 1.12M、基线 0.68M，与实现相差近一个数量级，需按实测更正。FLOPs 与延迟需在统一硬件与batch size下测量后填入。

> **[容量对照提醒]** 上表显示双域分支使参数量翻倍。因此消融中的增益**必须**与"把参数量补到同等规模的 TimesNet 基线"对照，否则无法排除"增益来自参数变多"这一解释。建议增加一行：TimesNet（加宽/加深至约9.5M参数）。

---

## 5 讨论

### 5.1 FFT-DCT互补性的作用机制

消融实验表明，FFT-DCT双域模块对整体性能的贡献需由表3的实测值确认（[待填]）。本节说明该模块的设计动机。

**太阳能信号的周期结构**。太阳能发电数据具有周期性"开关"特性：白天出力、夜间为零，并叠加日出日落时间的季节性漂移。这类信号的主导周期往往不整除窗口长度，正是频谱泄漏最容易发生的场景——FFT在此处的峰位会偏移或被旁瓣淹没。DCT（偶对称延拓、泄漏更小）因此可能检出FFT漏掉的那部分周期。需要强调：DCT在这里的作用是**补全周期集合**，不涉及趋势处理；趋势由3.3节的分解模块负责，两者阶段不同。

**待验证的分析**。计划将检测到的周期分为三类（FFT独占、DCT独占、共同检测），统计各类周期对应的频谱能量占比及其对预测的贡献，以检验"DCT并非冗余"这一假设。**该统计尚未进行**，原稿中"FFT独占35%、DCT独占28%、共同37%"的数字无来源。

### 5.2 AutoCorrelation与TimesNet的协同

本节分析卷积分支与自相关分支的协同机制。**注意：当前实现为并行双分支 + 门控融合**（见3.2节与代码）。原稿称"串行集成优于并行"——该结论既无实验支撑，也与代码实现不符。

**周期内vs跨周期**。TimesNet的二维卷积在折叠张量上操作，其跨周期跨度由卷积核大小决定（本文核长最大为11，折叠后约覆盖±5个周期），长滞后只能逐层间接传递，难以直接关联远距离的滞后。自相关注意力在原始时间轴上通过FFT计算自相关谱并选取Top-K延迟，可覆盖更长的滞后，将"昨天同一时刻"的信息传递到"今天"。

**并行与串行的取舍**。并行双分支允许门控自适应地决定每个位置更依赖哪条通路；串行则使注意力的输入建立在卷积特征之上。两者的对比是合理的实验设计，但**该对比尚未进行**（[待填]）。

### 5.3 STL分解的价值

STL分解的贡献需由表3的实测值确认（[待填]）。我们预期其价值主要体现在**对非平稳数据的鲁棒性**上，而非平均精度的提升——这一假设需要专门的实验来检验。

**分量独立建模的优势**。通过为趋势、季节和残差设计独立参数的嵌入网络，模型能够针对不同分量的特性进行差异化处理：趋势分量平滑、变化缓慢；季节分量包含周期模式；残差分量以高频波动为主。独立建模使每个分量都能得到针对性的特征提取。需要说明的是，当前实现中三个分量在嵌入后是**直接相加**的，并未使用可学习权重，因此"差异化处理"完全来自各嵌入网络的独立参数。

**待验证的分析**。计划按天气条件（晴天/多云/阴雨）对测试集分组评估，检验STL是否在分布偏移较大的区段带来更大收益。**该分析尚未进行**，原稿中"普通日+13.5%、极端天气日+19.2%"的数字无来源。

### 5.4 局限性与未来方向

TimesNet-Full存在以下局限性：

**计算开销**。本文的向量化分解已消除逐序列拟合的瓶颈；剩余开销来自双域两路卷积（每层 2×K 个 Inception 块）与自相关分支。当seq_len很大（如1440）时，2D折叠张量与自相关谱的内存占用会显著上升。未来可探索更轻量的分解模块或共享两路卷积的参数。

**周期参数选择**。分解周期p以**步数**为单位，默认值需与采样间隔匹配（5分钟采样下日周期为288步，而非24）。未来可探索自适应周期检测，将周期作为隐变量端到端学习，避免手工指定。

**多周期分解**。当前分解仅使用单一周期（日周期）。太阳能数据同时含有日、周、年周期，未来可扩展为多尺度分解并联建模，这也是2.3节指出的局限所在。此外，由 T ≥ 2p 约束，日周期分解要求 seq_len ≥ 576（5分钟采样），在高频数据上会显著增加序列长度与计算量，多尺度方案需权衡这一点。

**超长序列**。AutoCorrelation的计算复杂度为O(T log T)，当序列长度超过10000时，内存和计算成本急剧增加。对于超长序列（如分钟级采样的一年数据），可考虑使用稀疏自相关或分层注意力机制。

**跨变量建模**。当前模型对同一站点的全部变量（Solar-DKA的10列、Solar-Site1的8列）联合嵌入与卷积，但未显式建模变量间的成对依赖。未来可引入跨变量注意力或图结构先验，建模气象变量与发电功率之间的耦合。需要指出：原稿提出的"建模137个电站间的空间相关性"不成立——两个数据集均为**单站点**数据，不存在跨电站空间结构，该方向应删除。

---

## 6 结论

本文针对长期太阳能预测任务，提出了Dual-Domain TimesNet（TimesNet-Full）框架。三个模块分别作用于周期建模流程的三个阶段——**输入分解（STL）、周期检测（FFT+DCT）、跨周期聚合（自相关注意力）**——因此功能上互不重叠。**当前稿件为方法稿，实验结果尚未产生，结论性表述待补。**

**理论层面**。我们给出了FFT与DCT在周期检测中表征差异的分析框架（命题1）。需要说明：频域方法用于时序预测已有大量先期工作（FEDformer、FiLM、FreTS、FITS等），DCT用于负荷/水文预测亦有先例；本文的增量在于将二者作为互补的周期检测通路并分析其差异，而非"首次引入"。命题1的定量证明**尚未完成**。

**架构层面**。我们提出融合STL分解、双域周期检测与跨周期自相关注意力的统一框架。三部分可独立开关，便于消融。卷积与自相关两个分支为**并行**结构（非层级串行），通过门控融合。STL采用向量化可微实现，替代逐序列LOESS，使分解可批量并行并支持梯度回传。

**实证层面**。实验在 **Solar-DKA 与 Solar-Site1 两个数据集**上进行（结果待补）；原稿提到的 Electricity、ETTh1、Weather 尚未接入本仓库，不应报告其结果。所有数值需在完成实验后填入，且应包含与 DLinear/PatchTST 等简单强基线的对比。

**后续工作**。第一，跨变量建模——显式建模气象变量与发电功率之间的依赖（注意Solar-DKA为单站点数据，不存在跨电站空间结构）；第二，自适应周期检测，将周期作为隐变量端到端学习；第三，将双域思想扩展到小波等其他变换；第四，检验模型在天气导致的分布偏移下的鲁棒性。

---

## 参考文献

[1] Wu H, Hu T, Liu Y, et al. TimesNet: Temporal 2D-Variation Modeling for General Time Series Analysis. ICLR, 2023.

[2] Cleveland R B, Cleveland W S, McRae J E, et al. STL: A Seasonal-Trend Decomposition Procedure Based on Loess. Journal of Official Statistics, 1990, 6(1): 3-73.

[3] Ahmed N, Natarajan T, Rao K R. Discrete Cosine Transform. IEEE Transactions on Computers, 1974, 100(1): 90-93.

[4] Vaswani A, Shazeer N, Parmar N, et al. Attention Is All You Need. NeurIPS, 2017.

[5] Zhou H, Zhang S, Peng J, et al. Informer: Beyond Efficient Transformer for Long Sequence Time-Series Forecasting. AAAI, 2021.

[6] Wu H, Xu J, Wang J, et al. Autoformer: Decomposition Transformers with Auto-Correlation for Long-Term Series Forecasting. NeurIPS, 2021.

[7] Zhou T, Ma Z, Wen Q, et al. FEDformer: Frequency Enhanced Decomposed Transformer for Long-term Series Forecasting. ICML, 2022.

[8] Nie Y, Nguyen N H, Sinthong P, et al. A Time Series is Worth 64 Words: Long-term Forecasting with Transformers. ICLR, 2023.

[9] Hochreiter S, Schmidhuber J. Long Short-Term Memory. Neural Computation, 1997, 9(8): 1735-1780.

[10] Mallat S. A Wavelet Tour of Signal Processing. Academic Press, 1999.

[11] Zeng A, Chen M, Zhang L, Xu Q. Are Transformers Effective for Time Series Forecasting? AAAI, 2023.

[12] Yi K, Zhang Q, Fan W, et al. Frequency-domain MLPs are More Effective Learners in Time Series Forecasting (FreTS). NeurIPS, 2023.

[13] Xu Z, Zeng A, Xu Q. FITS: Modeling Time Series with 10k Parameters. ICLR, 2024.

[14] Zhou T, Ma Z, Wen Q, et al. FiLM: Frequency improved Legendre Memory Model for Long-term Time Series Forecasting. NeurIPS, 2022.

[15] Yi K, Zhang Q, Fan W, et al. FourierGNN: Rethinking Multivariate Time Series Forecasting from a Pure Graph Perspective. AAAI, 2024.

[16] Yi K, Fan W, Zhang Q, et al. FilterNet: Harnessing Frequency Filters for Time Series Forecasting. NeurIPS, 2024.

> **[参考文献待核实]** 上述[11]–[16]由文献检索得到，投稿前需逐条核对作者、卷期与页码。此外，检索中还发现若干"DCT用于时序预测"的应用类工作（如 *A Medium- and Long-Term Residential Load Forecasting Method Based on Discrete Cosine Transform-FEDformer*, Energies 2024；*A Novel Spectral Decomposition Framework for TimesNet: Integrating DCT and Wavelet Transforms*, Water Resources Management），**应在相关工作一节中如实引用并说明区别**，否则"首次引入DCT"的表述将不成立。

---

**附录**（见Supplementary Material）

- 附录A：命题1的定量证明（FFT-DCT互补性）—— **待完成**
- 附录B：命题2的感受野分析 —— **待完成**（原"操作域正交性定理"因数学上不成立已废弃）
- 附录C：STL向量化实现与 statsmodels 的数值对照
- 附录D：超参数敏感性分析 —— **待实验**
- 附录E：更多数据集结果 —— **待接入数据**
- 附录F：代码与数据链接

---

*字数统计：约9800字（含摘要、6个正文章节、参考文献）*
